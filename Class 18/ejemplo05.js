/* Links arrays if they have the same name, 'one' with red, etc. */

const colors = ['one', 'two', 'three'];

const [red, yellow, green] = colors;

console.log(red);

/* Destructuring is a technique to create and extract numerous variables simultaneously. */