const Header = (prop) => {
    return (
        <header>
            <h1>Examples in REACT</h1>
        </header>
    )
}

export default Header; /* Green words are COMPONENTS. Always in CAPS the first letter. */