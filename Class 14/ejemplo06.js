const frutas = ['Manzanas', 'Bananas', 'Palta', 'Peras', 'Mandarinas'];

/* El i = 0 hace referencia al primer valor de la lista. */

for(i = 0; i<frutas.length; i++) {
    document.write('Tenemos' + ' ' + frutas[i] + '<br>');
}